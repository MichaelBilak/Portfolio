/** Starting prices (EUR) for the order page — final quote depends on scope. */
export const orderPricingEur: Record<string, number> = {
  "premium-site": 2500,
  redesign: 1500,
  "booking-flow": 800,
  "monthly-support": 200,
  "photo-video": 600,
};
